stages:
  - trigger

variables:
  PIPELINE_TYPE:
    value: ''
    options:
      - ''
      - 'infrastructure'

infrastructure:
  stage: trigger
  trigger:
    include:
      - local: infrastructure/.gitlab-ci.yml
  rules:
    - changes: 
        - infrastructure/**/*
    - if: $PIPELINE_TYPE == "infrastructure"
