stages:
  - trigger

infrastructure:
  stage: trigger
  trigger:
    include:
      - local: infrastructure/.gitlab-ci.yml
  rules:
    - changes: 
        - infrastructure/**/*

applications:
  stage: trigger
  trigger:
    include:
      - local: applications/.gitlab-ci.yml
  rules:
    - changes: 
        - applications/**/*
